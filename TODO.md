1. Scan for YAW -> and use it to properly compute camera rotation
2. ~~Replace pointer chains with signature scanning~~ (done in v1.2.0 via `memory_resolver.rs`)
3. Add config file override for manually discovered offsets